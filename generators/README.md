The scripts in here are for generating various cache files from the API and don't interface with the normal functioning of the site.

Any script here was written for Python 3 and requires the requests library.